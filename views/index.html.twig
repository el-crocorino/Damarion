{% extends "layout.html.twig" %}

{% block title %}Start{% endblock %}

{% block content %}

    {% for question in questions %}

        <article>

            <h2><a class="questionTitle" href="/question/{{ question.get_id }}">Question {{ question.get_id }} : {{ question.get_text }}</a></h2>

        </article>

        <?php
            /*var_dump($games);
            var_dump($answers);
            var_dump($users);
            var_dump($votes);*/
        ?>

    {% endfor %}

{% endblock %}
